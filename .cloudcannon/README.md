# Testing RCC V2

- We have linked the `rcc-v2` package via GitHub.
- Node modules must be kept uncached for now.
- We need to delete `node_modules`, `package-lock.json`, and run npm install again locally. Then push to here.